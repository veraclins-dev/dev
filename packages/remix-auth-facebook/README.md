# remix-auth-facebook

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build remix-auth-facebook` to build the library.

## Running unit tests

Run `nx test remix-auth-facebook` to execute the unit tests via [Vitest](https://vitest.dev/).
