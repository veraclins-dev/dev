# remix-auth-google

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build remix-auth-google` to build the library.

## Running unit tests

Run `nx test remix-auth-google` to execute the unit tests via [Vitest](https://vitest.dev/).
