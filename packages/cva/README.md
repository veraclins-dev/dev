# cva

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cva` to build the library.

## Running unit tests

Run `nx test cva` to execute the unit tests via [Vitest](https://vitest.dev/).
