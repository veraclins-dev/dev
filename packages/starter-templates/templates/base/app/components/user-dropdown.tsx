import type { User } from '../utils/db/types'

/** Stub: replace with real UserDropdown for profile menu. */
export function UserDropdown({
	tooltip,
	profile,
}: {
	tooltip?: string
	profile: User
}) {
	return null
}
